openapi: 3.0.0
info:
  title: Invalid API
  version: 1.0.0

paths:
  /invalid:
    get:
      # Missing required responses field
      summary: This operation has no responses
      operationId: missingResponses
